# Rate limit global — incluir em /etc/nginx/conf.d/ (contexto http).
# Auth: ~10 req/min por IP; demais /api/: ~120 req/min.

limit_req_zone $binary_remote_addr zone=brfut_auth:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=brfut_api:10m rate=120r/m;
